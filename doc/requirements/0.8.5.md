# 0.8.5 需求文档：统一 Page 架构（cg.page）

> 状态：**R1~R10 已落地；R11~R18 经 RFC 评估 + v1 评审（22 条）+ v2 评审（21 条）
> 三轮迭代定稿，待实施**。
> **开发唯一依据 = [0.8.5_dev.md](0.8.5_dev.md)**（架构图/精准定义/签名总表/
> PageType 模板）；本文档 = 任务清单 + 验收 + 决策备查。
> 实施现场：test_res002（框架共建验证场）。
> **用户拍板（最高决策）**：① 一口气做好不留返工；② 不做向后兼容，老路径/老字段/
> 旧事件名/旧签名删除不留双轨；③ 一种 `cg.page({...})` 结构打天下，一切显示都是
> Page；④ 全类型注册后一律 closed（直接不显示），open 才显示；⑤ 四概念
> Box/Page/Widget/Cross，无子分类，混合体彻底分离；⑥ 双文档拆分，每个字有真实作用。

## 执行任务清单（优先级 = 依赖序 + 风险序，自上而下执行）

### P0 框架根基（新旧双轨并存，零破坏：page 内核与 widget 库同享切换纪律，dev §8.4）

| # | 任务 | 内容要点 | 验收 |
| --- | --- | --- | --- |
| T1 | `cgui/page.lua` 统一 Page 内核（低风险面） | `cg.PAGE` 九档（MAP=100/WORLD=10000 启用原死档）；def 白名单校验；生命周期（注册即 closed、on_init 恰好一次、**重注册不重置 on_init**、on_open/on_close）；queue/exclusive（同类型 exclusive 互斥）/suspend/group；`cg.page.*` 全家；事件更名 `cgui.page_opened/page_closed`；**check_z 整体重写**（校验对象 = type 枚举整档值 + 框架 root_z；页内 z 为相对序不入校验；旧带内偏移规则随档内偏移一并废除）；root_z/渲染序框架自动；panel.lua 并存不动（dev §3） | 真机：注册/开关/互斥/挂起/排队/页组全通；on_init 恰好一次且重注册不重置；toggle 无多重触发；首帧无闪显 |
| T1b | backdrop（T1 最高风险点单列，紧随 T1） | 全屏遮罩 + 输入吞噬 + 挂起随视图消失 + **与 dbg occlusion_provider 打通**（AI 对被遮下层正确报遮挡）（dev §3.1） | 遮罩可见/吞噬下层点击/关闭恢复；find_ui 对被遮控件报遮挡（occluded_skipped） |
| T2 | Widget 新库（双轨） | **新建** `libs/client/widget/` 新签名库：box.lua 容器重组（flow/grid/stack/scroll/pin/center/fullscreen/window）+ 全组件签名总表改造 + 回调四层命名表与缺口补齐（on_press/on_release/on_scroll/on_focus/on_blur/on_anim_event；on_select/on_tab→on_change；drag_ghost 源端→on_release；on_web_message→on_message）；**旧 `cgui/widget/` 原样并存至 T8；`api/cgui.lua` 的 cg.* 仍绑旧库**（同名冲突，cg 表只能绑一套）（dev §4/§5/§8.4） | 新库组件逐项真机过（临时探针页驱动）；现存全部 UI 零破坏 |
| T3 | tag + scope + raw + 状态服务新实现入库 | tag 贯通 core→dbg 快照→find_ui；**find_ui 真加 scope 参数**（页名前缀过滤，名实相符——现状无 scope 形参，q 子串顺带前缀）；`cgui/raw.lua` 逃生口；remember/tween 新签名**实现入库**（`cg.remember(initial, {key?,persist?})`/`cg.tween(target, {key?,...})` key 自动编号）**但 cg 绑定不切**——新旧签名无法可靠重载（initial 与 key 撞型），绑定切换归 T6.0 统一切换点（dev §5.3/§6/§8.3/§8.4） | tag/scope 查询链路通；新状态实现经模块级调用验证；现存 UI 零破坏 |

### P1 框架 Page（统一架构无例外，用新库写=第一批真机验证场；完成即切换纪律第 1 步全绿点）

| # | 任务 | 内容要点 | 验收 |
| --- | --- | --- | --- |
| T4 | toast/dialog/guide 四概念分离 | Cross 投递/状态 + Page 显示（`libs/client/page/{toast,dialog,guide}.lua`）+ Widget 页内组合（composite/dialog.lua）；dialog_host 归并；patterns.lua 停用待删；框架页模块级 require 新库（dev §7.3/§8.4） | toast/确认框/引导蒙层真机功能不变 |
| T5 | 调试台 cg.page 重构 | `libs/client/page/bench/`（cgui_bench + imgui_bench）；**debug_hub 按 §7.2 拆为两个标准 Page：独立 HUD 入口页（常驻，与游戏 HUD 页共存、锚点自避）+ BENCH 面板页（toggle 驱动）**；imgui_bench 探测内容经 `cg.raw`；统一 Widget 注册表，调试台 Widget 页自动列出框架+游戏件（仅元数据，选中才实例化）（dev §7.1/§7.2/§7.4） | 两台全页面回归：每页 ≥1 次 find 命中 + ≥1 次操作生效 + ≥1 次断言；debug_hub 入口与游戏 HUD 共存互不干扰 |

### P2 游戏迁移 + 模板（**原子切换批**：批首切 cg 表，批内逐页迁移恢复，批末全绿；每个功能一条任务防漏做；完成即切换纪律第 2 步全绿点）

> **批次形态（v4 评审定稿）**：T6.0 批首一次性把 `api/cgui.lua` 的 cg.* 切到
> 新库（含 remember/tween 绑定）——切换后游戏旧 UI 暂时全坏属**批内中间态**
> （仪器已在 P1 常绿，游戏页逐页迁移恢复）。迁移页**直接写 cg.* 终态**
> （与 dev §11 模板一致），不写模块级过渡形态、无二次改写。

| # | 任务 | 内容要点 | 验收 |
| --- | --- | --- | --- |
| T6.0 | **cg 表统一切换（批首）** | `api/cgui.lua` 的 cg.* 一次性切到新库（含 remember/tween 绑定切换）；旧库/旧 state 文件保留（T8 才删）；**同批先摘除旧视图挂载/注册点（启动编排区），再逐页以新形态恢复**——否则 8 个旧常驻视图 + 4 个旧面板的 render 每帧报错（cg.col/row 变 nil），errors 刷屏拖慢真机迭代；摘除后错误风暴从「每帧每视图」降为零 | build 零错误；摘除后无每帧报错刷屏；仪器（调试台）仍绿 |
| T6.1 | 背包页迁移 | BagUI → `page/popup/bag.lua`（exclusive+backdrop，页名小写）；结构迁移为主（背包样式已有基础） | 背包全开/关/拖放/购买链路真机回归 |
| T6.2 | 商店页迁移 + **样式重构美化** | ShopUI → `page/popup/shop.lua`（exclusive+backdrop）；迁移同时重做视觉（现样式太粗糙） | 商店全链路回归 + 真机视觉评审过 |
| T6.3 | GM 页迁移 + **样式重构美化** | GMUI → `page/popup/gm.lua`（exclusive+backdrop，dev §11.4 模板为基准再美化） | GM 发放链路回归 + 真机视觉评审过 |
| T6.4 | 组队页迁移 + **样式重构美化** | TeamHUD + TeamHudView → `page/popup/team.lua`（**非 exclusive**，与商店/背包共存）；邀请弹窗走队列族 | 组队/邀请链路回归 + 真机视觉评审过 |
| T6.5 | HUD 迁移 + **主界面重构美化** | HudBar/CombatHUD/ActionButtons → 独立 HUD 页（B 方案）；**接入 cgui 左摇杆（cg.joystick，替换现有输入通道）**；**布局考虑手机握持与刘海屏遮挡：HUD 元素 pin 锚定 + 左右安全边距统一避让**（安全边距常量进 GameConfig）；常驻 open 收拢启动编排区 | 摇杆移动/技能/攻击真机可用；手机比例视口下左右边缘无遮挡；HUD 全部件回归 |
| T6.6 | page/notify/ 迁移 | NotifyBar → NOTIFY 页 | 警告/toast 行为不变 |
| T6.7 | MAP/WORLD 拆分重写 | `page/map/MapView.lua`（瓦片整层，MapRender 渲染部分迁入）+ `page/world/`（RemotePlayers/Effects/FieldObjects/PlayerLife/HpBar 渲染 + 视口 culling）；**WorldState/MapRender（数据/碰撞/摄像机）/Sfx/BgmManager 归位 `src/client/world/`**（非 Page 子系统模块，目录纯组织）（dev §10） | 瓦片与实体/血条各自 Page 渲染正常；拖出视口的世界元素不渲染 |
| T6.8 | 游戏 widget 核验 | `src/client/widget/`（action_button 等）新签名 + use_widgets 单轨 | 游戏件可用；同名冲突游戏优先 + 告警可见 |
| T6.9 | 装配点收尾 | GameScene/GameClient/init.lua require 残留全量扫描（各页迁移时已随文件挪位同步改写，本任务扫尾）；**T4/T5 框架页的模块级 require 顺手统一为 cg.* 终态**；test/ 三测试文件迁移或清理 | build + 起局零错误；全仓无模块级直调 widget 库残留（除内核/文档特许） |
| T6.10 | 场景脚本 id 前缀全量更新 | 页名小写化改变 cgui id 路径首段，存量 run_scenario 用例里 `ShopUI/...`、`GMUI/...` 前缀断言全部失效——全量更新并跑绿；项目 AGENTS 速查表同步 | 存量场景脚本全绿 |
| T7 | src_template 同步（**必须在 P3 删除前**——模板在用 cg.patterns.form_page 与 cg.panel.register + 目录推导，删除瞬间模板即坏） | `src/client/page/` 目录 + POPUP/HUD 两起手式样板（dev §11 节选）+ README 动线 | 模板 build 零错误 |

### P3 清理（两轮全绿后才删）

| # | 任务 | 内容要点 | 验收 |
| --- | --- | --- | --- |
| T8 | 删除旧物 | panel.lua、旧 `cgui/widget/`、patterns.lua、kit.register、旧事件名、路径推导机制、col/row/box 旧名、档内偏移写法 | grep 旧形态（`id_or_opts`/`button_primary`/`cg.panel`/`cg.kit`/`cg.LAYER`/`panel_opened`/`cg.col`/`cg.row`/`cg.box`/`on_select`）零残留；build + 起局零错误 |

### P4 配套与收口

| # | 任务 | 内容要点 | 验收 |
| --- | --- | --- | --- |
| T9 | editor-patch 配套 | lua.find_ui 加 scope/tag 参数（可选、前向兼容；游戏侧就绪后启用）（dev §8.4） | find_ui 按 scope/tag 命中（含多 tag 任一匹配）+ 返回 tags + crop 局部截图链路跑通（**验收前置：依赖 T6 已有带 tag 的真实 UI**） |
| T10 | 文档收口 | cgui.md 重写（唯一权威；layer/panel/Layout 字样清除）；cgui_mcp.md（scope/tag 检索）；两侧 AGENTS.md；**游戏侧 `src/doc/`（问题注意.md、cgui迭代答疑记录.md 等旧 API 表述清理）**；changelogs/cgui.md；RFC 文档标注「已评估迭代，结论见本文档与 dev」 | 文档与新 API 逐条对得上 |
| T11 | 知识沉淀 | 统一 Page 架构终验结论与实测发现落盘 `doc/research/` | — |

**优先级理由**：P0 不立则一切免谈，且双轨并存零破坏（page 内核与 widget 库同享
切换纪律——v3 评审修正：widget 就地改造会让全部现存 UI 立即坏掉，违背 P0 承诺）；
P1 用框架自身 Page（toast/调试台）当第一批真机验证场——调试台是 AI 闭环的工具面，
先绿它后面排障才有仪器；P2 游戏迁移是最大改动面，放在仪器齐备后，src_template
同批（它是 P3 删除项的在用方）；P3 删除必须在两轮全绿之后；P4 配套与文档最后收口
（文档与功能同提交纪律在任务粒度上体现为：每个任务交差时同步其直接文档）。

## 验收总表（真机，test_res002）

- 链路：`bgd_sce_tools build` + MCP `stop_debug/start_debug`（秒级迭代），场景脚本一把跑；
- **挂起遮挡回归**（R1~R3 语义不变量）：调试台/全屏商店打开时 find_ui 找不到下层
  控件、tap 报 actionable 错、有 `occluded_skipped` 回显、capture 无下层渲染；
  关闭后恢复；挂起/恢复往返后 exclusive 仍互斥；同档次序/back 导航不失真；
- **统一 Page**：全类型注册后 closed、open 才显示；on_init 恰好一次且重注册
  不重置；exclusive 新语义（商店开→背包关、TeamHUD 非 exclusive 不受影响）；
  group 批量开关（关装备全关）；backdrop 遮罩可见且吞噬下层点击、关闭恢复，
  且 dbg 对被遮下层正确报遮挡；
- **MAP/WORLD 拆分**：瓦片与实体/血条各自 Page 渲染正常；culling 生效；
- **id 前缀连锁**：页名小写化后存量 run_scenario 用例 id 断言全量更新并跑绿；
- **AI 闭环零回归**：dbg 快照/虚拟指针/state 注入/scope 前缀/occluded_skipped
  全部功能等价（允许实现重写，语义不变）；
- 两处日志 **errors=0**。

## 决策备查（为什么这么定；评审逐条对照）

### RFC 评估结论（2026-08-31 三轮评估）

| RFC 诉求 | 结论 |
| --- | --- |
| 层级命名与目录映射对齐 | 已落地（R5/R7）→ 定稿 PageType / cg.PAGE / page/ 目录 / 路径推导删除 |
| 聚合目录 + init.lua 入口 | 已落地（R8.1，引擎原生目录语义） |
| 特性注入（互斥/挂起/排队/遮罩） | 互斥/挂起/排队已落地（R1/R2）；遮罩 → backdrop；exclusive 语义 v2 修订（dev §3.2） |
| 声明式数据树（Table DSL） | **不采纳**（引擎级理由：props2 签名即 diff / 裸表静默失效 / 结构性属性仅创建时生效 / 事件纯命令式必留逃生口）；其真正价值「单 props 表 + 具名回调」采纳（T2） |
| widget 平级扩展 | 已落地（R9）→ 归位 libs/client/widget/ + 无子概念 |
| 调试台迁移解耦 | 已落地（R10）→ 进一步 cg.page 重构 + 挪入 page/bench/ |
| base.ui 作基座（评审中提议评估） | **否决**：imgui 控件超集（26 vs 8）；0.8.* AI 闭环寄生每帧重组管线；RMGUI 内容经 bridge 逃生口嵌入 Page（唯一方向） |

### v1 评审（22 条）要点

族概念退役（PageType 给默认值 + 定义组合覆盖）/ 挂起实现允许重写 / 调试台与
src 全界面 cg.page 重构 / 事件更名 page_opened / 原生内容经 raw+wrapper 塞 Page /
不可见 Page 完全释放（现状即该设计：视图卸载零开销 + 模块局部表/持久 remember
恢复）/ 调用即绘制心智模型 / tag 检索 / 回调 on_ 全前缀不加 cgui 前缀 /
装备场景联动 / 签名总表真实文件位置 / 浮层属宿主 Page 不独立成页 /
无内置页例外 / Widget 归位 libs / 四概念 / layer→page 全面更名 / 删路径推导 /
dbg 闭环详细设计 + 切换纪律 / 每 PageType 一示例 / 双文档拆分。

### v2 评审（21 条）要点

Page 组织三形式 / 「档位」≠bind（保留档位，不附英文）/ **MAP·WORLD 分层**
（死档启用；HUD 不跟随地图坐标）/ §3.1 白话详解 / **档内偏移废除**
（exclusive 改同类型互斥）/ 注册即 closed = 直接不显示 + on_init +
toggle 无多重触发（逐路径核实）/ **with 废弃改 group 页组** / tag 多标记约定 /
回调四层表（子代理全量分析：补 on_press/on_release/on_scroll/on_focus/on_blur/
on_anim_event；drag_ghost 源端 on_drop 撞名改 on_release）/ view_size 归 Cross /
Widget 无子概念 + 调试台统一注册表自动注入 / 四概念定稿 Box·Page·Widget·Cross
（flow 合并 col/row、stack 更名 box）/ 逃生口归 Cross / raw 不进快照是技术限制
（引擎无 Lua 拦截点）/ **HUD 融合拍板 B 方案**（debug_hub 独立 HUD 页共存，
不搞特例通道；宿主槽位机制不做）/ easing·bezier·path_lerp
代评审（纯函数工具已最简，归组 Cross 动画）/ toast 等四概念彻底分离（无语法糖）/
dbg 实现允许重写（语义等价为前提）/ 性能红线查漏补缺 + 句柄泛化为 Widget 通用
能力 / src 地图按新分层重写（实战验证 + Tiled 铺路）/ 示例与 dev 冲突以 dev 为准 /
迁移清单适配。

### v3 评审（任务清单轮）要点

**widget 库与 page 内核同享双轨**（T2 就地改造会立即打坏全部现存 UI 与调试台
自举，违背 P0 承诺；新库先建、旧库 T8 才删）/
T1 拆分（backdrop 为最高风险点单列 T1b，含 dbg 遮挡打通）/ **T7 src_template
提前到删除前**（模板在用 patterns/panel.register，删除即坏）/ **T6 补全量子任务**
/ T9 验收前置标注（依赖 T6 带 tag 真实 UI）/ T10 补 src/doc/
实况修正入库：kit.lua 实为 1448 行 25 公开组件；find_ui 本无 scope 形参
（名实相符 = 真加参数）；check_z 随档内偏移废除整体重写；重注册不重置 on_init。
补充轮：T5.5 删除（debug_hub 两页重构本在 T5 内，写明独立 HUD 入口页 +
BENCH 面板页结构）；T6 按功能逐条拆分（背包/商店/GM/组队各一条，后三者
含样式重构美化）；T6.5 主界面重构美化（接入 cgui 左摇杆 + 手机握持刘海屏
安全边距避让，边距常量进 GameConfig）。

### v4 评审要点

**remember/tween 纳入双轨**（T3 就地翻转会同时打坏旧调试台/ShopUI/模板三处，
新旧签名 initial 与 key 撞型无法重载过渡）→ 推论出统一定律：**cg 表上的一切
签名变更归属同一个切换点**；**cg 表切换从批末提到批首**（T6.0，原子切换批）——
消除迁移页「模块级过渡形态 + 二次改写」破洞，迁移页直接写 cg.* 终态
（与 dev §11 模板一致）；「不中断」精确定义 = 每批结束系统可用、仪器常绿，
批内中间态可坏。编号修正：src_template=T7/删除=T8；T6 扩到 T6.0~T6.10。
补充轮：T6.0 加「同批先摘除旧视图挂载/注册点再逐页恢复」（防每帧报错风暴）；
「边界与不做」补两条已认可语义变化（debug_hub 入口随 HUD 档挂起 / 全局对话框
归位 SYSTEM）防误当 bug 执行；**任务级原子 git**（test_res002 本地 git 已有并
一直在用，每完成一项任务 commit 一次，不推远端）。

### 边界与「不做」（维持）

- 部分遮挡不做逐像素判定（本游戏全屏页是主流形态，主路径已闭合；边缘场景成本高）；
- NotifyBar root_z=60000 / PlayerLife root_z=150 行为保留（实测调出，只修文档表述）；
- remember 瞬态槽位 600 帧回收是已知边界（要「关再开不丢」走持久槽位/模块局部表）；
- on_suspend/on_restore 不加（挂起=不渲染动画自停，无真实场景；语义位置预留）；
- Tiled 完整运行时不做（本期只定接口形态，dev §10）；
- **已认可的语义变化（非 bug，勿当回归错误执行）**：debug_hub 入口是 HUD 档
  Page，会随 exclusive 全屏页打开被挂起（评审讨论拍板认可）；全局对话框页档位
  由 BENCH+100 归位 SYSTEM——调试台打开时系统弹窗不再压过调试台（评审讨论
  拍板认可）。

## 协作约定

- **改动范围**：`sce_app_editor-patch`（文档/报告 + find_ui tag 桥配套）+
  test_res002 的 `.bgd`（cgui 框架侧与游戏 UI 全量迁移）；
- **git 边界与原子提交**：sce_app_editor-patch 走 git + Conventional Commits；
  **任务级原子提交规则（v4 拍板）**：每个任务完成 = 一次原子提交（可回退粒度
  = 任务）。test_res002 本地 git 已有并一直在用（实查 log 含 0.8.3/0.8.5 提交）：
  **每完成一项任务就 commit 一次；不推远端**（正式版再建远程仓库与正式命名）；
- **切换纪律**（dev §8.4）：先新后旧、真机验证再退役，过程不中断、不改自闭；
- **框架共建**：改 `.bgd/libs` 同步评估 `.bgd/src_template/`（T7）；
- **文档同步同提交**：每个任务交差时同步其直接文档；T10 做总收口；
- **500 行拆分纪律**：模块超 500 行必须拆分职责，拆前全局理解再动手；
- **知识沉淀**：T11 落盘 `doc/research/`。
