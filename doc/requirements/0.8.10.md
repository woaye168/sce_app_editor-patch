# 0.8.10 需求文档：AI 易用性「轨道化」——让正确路径成为阻力最小路径

> 状态：**需求已收集，待评审排期**。
> 来源：2026-09-03~04 test_res002 全量测试（2 小时，AI 全程自主执行）的真实使用反馈 +
> 任务后三轮深度复盘对话。测试报告：test_res002 `.bgd/test/report.md`；
> 逐轮执行记录：`.bgd/test/checklist.md`。
> 本文档全部内容来自**真实使用中踩过的坑**，非想象需求。

## 背景：这次全量测试暴露的不是「能力缺失」而是「默认路径错误」

0.8.0~0.8.9 的能力面在 2 小时高强度真实使用中**全部可用**（find_ui/tap/drag_ui/eval/run_scenario/多人 player 定向/set_pause/分玩家日志/审计日志无一失效）。但过程中反复出现同一模式：

> **工具给了两条路，AI 选了快的那条，而快的那条恰好绕过了产品真实路径。**

具体表现：背包/商店/锻造/技能的逻辑层测试全部用 `lua.eval` 直发协议完成（快、确定、可统计），导致**背包 UI 在整整一轮测试里从未被真实打开过**——用户在旁观时亲眼看到「锻造提示在弹但背包界面根本没开」才发现。这不是 AI 性格问题，是任何 AI 面对「eval 一行搞定」vs「拖拽 20 步」都会做的选择。

**设计哲学（本轮最高约束）**：无法确保每个 AGENT 的性格与习惯，只能从工具链自身想办法——**让正确的路恰好是阻力最小的路**。不依赖 AI 自觉，不依赖提示词教育，靠工具结构与默认行为兜底。

## 需求清单

### R1 eval 逃生舱的边界显性化（description 双向界定）

**问题**：`lua.eval` 的 description 只说了能干什么（逃生舱、danger），没说不该干什么。AI 在真实需求压力下会拿它驱动业务协议（`protocol.send_to_server(P.Req_*)`）绕过全部 UI 验证。

**方案**（description 改写，零代码逻辑变更）：

```
lua.eval — 游戏 VM 逃生舱（danger 级，兜底通道）。
仅用于：① 无对应工具的运行时状态读取/断言；② 测试探针安装（链式包装
base.proto 捕获消息）；③ 统计性验证（概率/分布采样）；④ 构造 UI 无法到达的
边界态（满包/非法参数/断档）。
禁止用于：能用 find_ui/tap/drag_ui/key_down 完成的 UI 操作——UI 链路必须走
真实操作验证，eval 绕过 UI 的测试不算 UI 已测。
```

**配套**：eval 返回的 hint 增加反问引导——「该操作有对应 UI 入口吗？有则用 lua.tap 走真实路径」（hint 随能力集同步发版的既有纪律覆盖）。

### R2 run_scenario 内建「取证三段式」步骤类型

**问题**：正确的测试姿势（操作 → 截图 → 状态断言 → 日志锚点）是复盘后才总结出来的，新上下文的 AI 不会天然这么做。截图是「无预设的观察」，断言是「有预设的验证」——只有后者会漏掉「没想到要查的东西」（本轮 BUG-04 日志刷屏就是这么漏的）。

**新增步骤 op**：

| op | 语义 | 解决 |
| --- | --- | --- |
| `verify_ui {q, scope?, tag?}` | find_ui + capture_ui + 返回「UI 存在且长这样」一步完成 | 「UI 存在性验证」从三个动作变一个 |
| `check_logs {match?, errors_zero?: true}` | 查日志 + errors 段必须为 0 的断言（默认开） | AI 不再需要「记得查 errors」 |
| 场景结束自动附件 | run_scenario 跑完自动附「期间日志摘要：新增行数/errors/warnings/top 重复行」 | 刷屏类问题（BUG-04 型）天然暴露 |

### R3 取证覆盖率自检（证据计数器）

**问题**：本轮 Round 1 全程零截图，没有任何机制提醒。「画面没看过」应该是一个可检测的状态，而不是依赖用户旁观发现。

**方案**：`get_status` / run_scenario 结束返回附 `evidence` 段：

```json
"evidence": { "captures": 0, "ui_ops": 47, "eval_ops": 12 }
```

当 `eval_ops` 远大于 `ui_ops` 且 `captures == 0` 时 hint 显性提示：「本会话大量 eval 但零截图——若测的是 UI 功能，画面取证缺失」。实现 = 计数器，成本极低。

### R4 关键 UI 操作可选视觉回执

**方案**：tap/drag_ui 加可选 `snapshot=true`：操作后自动 crop 该控件区域截小图存证，返回路径。文本断言（expect）只能验「文本变了」，验不了「画面看起来对」；视觉回执让「每次关键操作留证据」成为默认动作而非额外负担。crop 局部图成本可控。

### R5 AGENTS.md「AI 调试闭环」段补「完成定义」

**问题**：现有文档是操作手册（怎么调工具），新上下文的 AI 更缺的是「做到什么程度算完」。

**方案**：test_res002 / 框架 AGENTS.md 的调试段加一段（落点：游戏项目 + 框架 libs/AGENTS.md）：

> 一个功能「测完」的最低标准 = ① 真实 UI 路径操作过（不是只发协议）
> ② 至少一张截图证明呈现正确 ③ 相关日志无 error ④ 双端状态经断言验证。
> 缺任何一个都不算测完。

### R6 服务端状态通道（独立设计，本轮只立方向）

**问题**：服务端（remote VM）不在 lobby 总线上，AI 只能用「日志锚点 + 客户端可观察状态」间接断言服务端行为——这是 eval 被大量使用的另一半真实原因（能力空缺）。服务端死亡/重生/击杀等关键路径零日志锚点，排障卡死（BUG-05/ANOM-01 排查都卡在这里）。

**方案方向（已定，待排期实施）**：客户端 dbg_bus 加 `server_eval` 转发命令——经 `protocol.send_to_server('Req_DbgEval', {code})` 发给服务端 dbg handler（仅 PIE 调试局注册），结果经 `Sync_DbgEvalResult` 回客户端再经 lobby 回编辑器。改动面：客户端 dbg_bus +15 行 / 服务端 +20 行 / 桥零改动（ui_loop 全参透传已就位）。安全：仅调试局注册的硬门（生产局无此协议），等价 eval danger 语义。

### R7 已修缺陷回同步提醒（非新需求）

本轮已在 test_res002 工作区修复 **vp 注入祖先扇出**（`dbg.lua`/`core.lua`/`dbg_commands.lua`：press/on_mouse_down/on_real_click/hover 扇出到交互祖先，修复长按拆分不可达 + click_at 点物品无效）——**当前未提交 git、未回同步框架**。0.8.10 实施时需先将该改动纳入正式版本（test_res002 提交 + 框架回同步评估）。

## 摩擦点清单（已记录，R1~R6 之外的存量待修项）

以下来自测试执行记录（checklist.md Round 1~6），均为真实踩坑：

| ID | 问题 | 建议修法 |
| --- | --- | --- |
| FR-05 | run_scenario 步骤缺 op 字段时报「未知 op: ''」不指出根因 | 报错改为「步骤缺 op 字段（op: invoke/wait/...）」 |
| FR-06 | tap/pick 不支持 scope/tag 参数（find_ui 有） | tap/pick 加 scope/tag 透传 |
| FR-08 | invoke_capability 的 player 放顶层（非 args 内）静默路由到 1 号（连犯 3 次） | mcp.rs 检测顶层 player 时兼容透传或响亮提示 |
| FR-09 | tap expect 3 帧验证窗口对「需服务端往返」操作太短（组队退出实测误报） | hint 区分：本地即时变化用 expect；服务端往返用 wait_for/assert_text |
| FR-02 | errors 段只上浮 [error]，[warning]（如 protocol duplicate register）不上浮——探针覆盖游戏 handler 曾因此漏看 | errors 段加可选 warns 段 |
| FR-07 | match 与 tail_lines 同用时看不到末尾原文 | 允许两者并返 |
| FR-10 | save_as 对 find_ui 结果未存变量（items[0].clickable_ancestor 存在但报「变量未定义」） | 修 extract_save 对 find_ui 结果形态的取值路径 |
| FR-03 | eval 返回混合键表时退化为 Lua 字符串序列化而非 JSON | 低优先级（可读） |
| 缺口 | 无 double_click 能力（背包双击旋转不可测） | 评估 vp 合成双击（两帧内两次 down/up）可行性 |

## 非目标

- 不改变任何现有能力的语义（全是增量：description 改写 / 新步骤 op / 计数器 / 可选参数）
- 不做「AI 使用规范」的提示词教育（纪律写进工具返回与结构，不靠 AGENTS.md 教学）
- R6 服务端通道本轮只定方向与改动面，实施独立排期

## 验收标准（实施时逐条）

- R1：eval description 更新后，新会话 AI 读 description 能复述「什么算滥用」
- R2：用 verify_ui + check_logs 重写一条背包测试用例，步骤数对比收敛
- R3：构造「全程 eval 零截图」会话，get_status 的 evidence hint 正确触发
- R4：tap{snapshot=true} 返回截图路径且图片内容与操作控件对应
- R5：全新上下文 AI 拿到修改后 AGENTS.md，测背包时第一步是打开背包界面
