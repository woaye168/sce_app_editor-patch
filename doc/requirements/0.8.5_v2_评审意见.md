@ 0.8.5_dev.md
1、
【你】「1. 架构图」「框架 Page   libs/client/page/（toast / dialog / guide / debug_hub /
│                  cgui_bench / imgui_bench —— 调试台同样是 Page，无例外）」
【阿叶】不管是libs还是src ，推荐 xx/client/page/<类型名>/<模块> 、 xx/client/page/<职责目录>/<模块> 、xx/client/page/<模块> 组织 Page。而框架的「toast / dialog / guide / debug_hub / cgui_bench / imgui_bench 」正是：xx/client/page/<职责目录>/<模块> 和 xx/client/page/<模块>

2、
【你】「2. 概念体系」「渲染上下的数值叫档位（band)」
【阿叶】应该是绑定(bind)更贴切吧？rmgui也是这么教的。

3、
【你】「3.1 PageType 与默认特性矩阵」「WORLD：二维世界内元素（瓦片地图/世界血条/场景 UI）」
【阿叶】你觉得要不要把 「瓦片地图」 和 「世界血条/场景 UI」分层隔开呢？ 因为 「瓦片地图」 会渲染很多的 图块，可能要对整个层做优化处理，还有未来要实现的Tiled完整运行时也是基于这一层。你觉得呢？（这里可能要结合 HUD 思考下，看看有没必要必要在中间新加一层）。我的设想：地图层 \ 地图UI层（需要基于地图层坐标定位的UI，有可能在可视区外。然后要做不渲染优化。比如 安置角色的spine模型，玩家角色和怪物血条，掉落的物品，飘字...） \ 常驻主界面层（玩家状态信息，玩家功能如入口按钮... 这些不会跟随地图层坐标的）

4、
【你】「3.1 PageType 与默认特性矩阵」「exclusive/backdrop 默认值全类型为 false。
suspend 唯一默认规则：exclusive = true 且自身档位 > HUD 档 → 默认 { cg.PAGE.HUD }（全屏独占页隐 HUD，R2.1 语义）；显式 suspend = false opt-out，显式数组覆盖集合。
10000~19999 为死档（check_z warn）；档内偏移 >999 warn。」
【阿叶】这些我都没看懂，请详细补充说明

5、
【你】「3.2 档内偏移（答评审③）」
【阿叶】你看看这个有没必要，实际上如果能做到完全自动管理（自动+1-1），似乎完全没有必要这条，你先审查下真实情况，再来看有没必要

6、
【你】「3.3 生命周期」
【阿叶】「注册后一律 closed」是指：A先显示在关闭，还是说B直接就不显示？ 是哪种？如果是B我没意见，如意是A的话请你先分析现状，判断是否可以实现成B？这样无论性能还是体验应该都更好吧？
生命周期钩子，你看看是否有业务场景缺失的，比如初始化钩子。另外还有个疑问？toggle(name, params)  和 open 和 close 钩子会不会有重叠导致多重触发的情况？请审查实际情况后，再回答我。我只是个疑问，不是反问。

7、
【你】「3.4 同伴页 with」
【阿叶】这个with功能是为了解open或者close互相引用循环的对吧？ api形式是不是应该设计成 key:声明周期钩子名  比较好？还有这里可能要好好思想实验考虑下，直觉这里弄不好就崩程序，或者引起奇怪的Bug。

8、
【你】「4. Widget 调用约定」「tag：语义检索标记」
【阿叶】这里tag是支持多tag的，要说清楚，以及约定形式。

9、
【你】「4.1 回调命名表」
【阿叶】直觉这里有问题，麻烦这里你交给子代理思考下，看看能不能获得更有价值的建议。我的感觉是似乎覆盖不全。还有有可能事件会有职责同类化。

10、
【你】「5.1 Layout」「view_size」
【阿叶】这一个感觉很奇怪，有点特殊化，你看看是否能合理的安置下新架构中统一化处理。现在文档中它的位置其他的都是容器，这个完全不像是容器。

11、
【你】「5.2 Widget·叶子」「5.3 Widget·控件」「5.4 Widget·列表/图集」「5.5 Widget·复合件」
【阿叶】新架构中，这几个能否合并就一个「widget」，现在相当于搞成了3个概念，认知负担，你结合实际情况看看，再回答。另外我想到，在「cgui调试控制台」也应该别搞什么内置组件\扩展组件\游戏件\这么多概念，就是widget，然后最好能是自动注入到「cgui调试控制台」的「widget」里（别忘了考虑性能）。

12、
【你】「5.1 Layout」
【阿叶】我们应该修正四概念（Box（容器）、Page（界面）、Widget（控件）、Cross（横切服务）），容器表达更贴切，而且Layout表达会和Widget纠缠不清。，全文都不要再有Layout，而现有的 Box（容器），你可能要思考下是不是覆盖完全业务场景。而「col / row / box / scroll」里的「box」可能要分离成「flow 和 guid」。

13、
【你】「5.6 逃生 Widget」
【阿叶】感觉这个概念上应该不属于 Widget。而是属于cgui的api。

14、
【你】「5.6 逃生 Widget」「raw（新增）」
【阿叶】边界：「raw 内控件不过 core.begin，不进 dbg 快照、不可被 find_ui/vp 操作」，我想知道在这些是约定还是说技术不可实现？如果是技术不可能实现，那么我认为OK，如果是约定，我想知道为什么要这么约定？是影响性能还是什么？从我的想法上来讲，能支持当然是更好的，比如能支持快照，就能AGENT自动排查。（当然这里如果实现很复杂，也可以不用考虑，因为我想实际使用的情况也不会很多，我甚至暂时都想不到要使用的场景，因为所有的代码都会重构使用新的cgui架构和api）

15、
【你】「5.8 Page 入口」「§3 全部。另：cg.debug_hub 保持（调试入口坞，自身重构为 Page，见 §7.3）。」
【阿叶】为什么要「cg.debug_hub 保持」？不是新架构已经有合适的分层（HUD）安置了吗？应该安置到libs的HUD里，不过因为主界面只会有一个，所以这里HUD应该做成做位置注册融合，这里你看看技术实现，然后再和我确认。

15、
【你】「6. 状态服务重构」「旧三参/两参形态删除；easing/bezier/path_lerp 不动。」
【阿叶】「easing/bezier/path_lerp 不动。」这里我不太懂，但是但凡以后有「不动」这样的地方，你可能都要代替我评审下，新架构有没更优雅的实现方案，咱们不要图省事。完美之心。但是再次提醒切记：五性约束：规范性 / 一致性 / 易用性 / 性能 / 扩展。

16、
【你】「7.1 目录与归位」「便捷函数保持（语法糖，不是第三类结构）」
【阿叶】我觉得语法糖没必要，因为始终使用Page，这样使用者想要修改也很方便，同一特性的还能通过多个Page来实现不同的样式。可以抽到libs的 Widget中，然后由Page显示。但是要注意了：千万别搞兼容，就是正常widget和page,所以现有的这些 toast 这些肯定是要改的，现在这个实际上是集有widget有是Page。要完全按照新架构的 四概念分离！！！切记，切记！

17、
【你】「8.2 新架构下的不变量（零破坏）」
【阿叶】从我的考虑是，一切为新架构考虑，如果能在新架构下达成有更优雅且遵循五性约束的情况下，那么是让现有的退役重写的。

18、
【你】「9. 性能红线」
【阿叶】这条请你再次结合实际情况和前面所有我的评审，查漏补缺。还有想「句柄型组件（joystick/pscroll）返回稳定身份句柄，每帧调用零额外成本。」这种应该考虑的是 「Widget」能力泛化。

19、
【你】「10. Tiled 地图运行时预留」
【阿叶】Tiled完整运行时是后面的计划。但是现有的src的地图这些都需要用新架构来重写，写到正确的分层上。这样本身也是在实战调试验证新架构。（我之所以告知计划，是为了你本版本实现能更接地气的考虑到未来情况来实现，尽量让未来做「Tiled完整运行时」不会因为结构性问题大概或者做丑陋的兼容。 ）

20、
【你】「11. PageType 模板示例」
【阿叶】结合我前面的评审，你看看这些示例有没有要修改的，有则做调整。另外在这个章节注明，如果示例代码与「0.8.5_dev」规则不相符，应该尊重开发文档。

21、
【你】「12. 迁移清单（执行顺序即依赖序）」
【阿叶】结合前面的，请你做出适配更新调整。
