# 0.5.3 测试用例

> 目标：场景一「开发调试链路」体验完整性（doc/requirements/0.5.3.md，M0–M5）

## 单元/集成（cargo test）

| # | 用例 | 预期 |
| --- | --- | --- |
| U1 | slot_inject：insert_slot 顶层 return 前注入 / 无 return 追加 / transform_unlock 只解禁顶层 `x = nil` 行 | 通过 |
| U2 | bridge_deploy：deploy/undeploy/restore 幂等 + 跨版本旧登记键前缀清理 | 通过 |
| U3 | kernel：精确插槽端到端（199/160）apply→check→restore 字节级还原 | 通过 |
| U4 | kernel：v999 无插槽 → 运行时注入（入口含 INJECT_MARK、isolation 含 UNLOCK_MARK、还原回加密原文） | 通过 |
| U5 | slots：版本枚举 versions_below(169) 最近为 160 | 通过 |
| U6 | editor.rs（tools）：civil_from_days / read_tail 截断 / latest_file mtime 优先 | 通过 |

## 真机（编辑器 + MCP 链路）

| # | 用例 | 预期 | 结果 |
| --- | --- | --- | --- |
| E1 | `bgd_sce_tools logs --project bgd_glzy` | 五类日志源（client/server_core/server_lua/bridge_main/bridge_audit）返回最新文件路径+大小+创建/修改时间+行数，无内容 | ✅ |
| E2 | stdio MCP initialize / tools/list / get_logs | NDJSON 正常应答，8 工具清单 | ✅ |
| E3 | 离线 start_debug | 明确报错「编辑器不在线…请先 editor_start」 | ✅ |
| E4 | `editor start --project test_res002` | 拉起编辑器并等桥上线，返回 pid/port/mcp_url（端口避让实证 39177→39273） | ✅ 73.5s |
| E5 | MCP get_status | map_path=test_res002，debugging=false | ✅ |
| E6 | MCP start_debug（restart_last_debug） | debugging=true（跳过编辑器编译构建） | ✅ |
| E7 | 旧版桥调 lua.capture_game | 干净报错 NOT_FOUND「能力不存在」（错误路径验证） | ✅ |
| E8 | MCP stop_debug | debugging=false | ✅ |
| E9 | `editor stop` | server_info 取 pid → taskkill /T /F → 进程消失（静默端口文件残留由 ping 双重确认兜住） | ✅ |
| E10 | 新 dll+lua 部署后 capture_game 真机截图 | 返回 png 路径且画面正确 | ⏳ 待部署后验证（沙箱写白名单外，需人工复制） |
| E11 | publish_project 真机发布（danger 放行后） | promise 返回 code=0，事件 publish_done | ⏳ 同上（副作用：会上传创作者中心，选做） |

## 说明

- E10/E11 依赖把新构建的 `bgd_mcp_bridge.dll`（→ `D:/sce_online/version-13/`）与新 `main.lua`（→ xdeditor/169 补丁目录）复制到编辑器目录；本会话沙箱写白名单不含该目录，需人工执行两条 Copy-Item 后重启编辑器验证。
- F1（项目切换自动刷新注入路径）验证：切换项目后重开编辑器补丁应用，状态栏提示已自动更新；编辑器内 unwatch 行为随重启生效。
